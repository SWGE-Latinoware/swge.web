import EnumUtils from '../utils/EnumUtils';

const UserRoleType = EnumUtils.Enum(['ATTENDEE', 'SPEAKER', 'CARAVAN_COORDINATOR', 'SECRETARY', 'DPO', 'GRID_COORDINATOR']);

export default UserRoleType;
